# Production VITE API URL for Vercel deployment
# Points at the stable server alias (server-git-main). The frontend domain does NOT proxy /api/v1.
VITE_API_URL=https://server-git-main-nelgoezs-projects.vercel.app/api/v1
