{
  "version": 2,
  "bunVersion": "1.x",
  "buildCommand": "bun install && mkdir -p .context/SRS && cp ../.context/SRS/api-contracts.yaml .context/SRS/api-contracts.yaml && cp ../.context/SRS/api-contracts.yaml src/api-contracts.yaml",
  "installCommand": "bun install",
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }],
  "crons": [
    {
      "path": "/api/v1/cron/expire-overrides",
      "schedule": "0 0 * * *"
    }
  ]
}
